<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hhr_project">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HHR_Project"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".Search"
            android:exported="false">
        </activity>

        <!-- Make Login the entry point activity -->
        <activity
            android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Update other activities -->
        <activity
            android:name=".myReservations"
            android:exported="false" />
        <activity
            android:name=".Seat"
            android:exported="false" />
        <activity
            android:name=".Personalinfo"
            android:exported="false" />
        <activity
            android:name=".roundTrip"
            android:exported="false" />
        <activity
            android:name=".ReserTest"
            android:exported="false" />
        <activity
            android:name=".SignUp"
            android:exported="false" />
    </application>

</manifest>
